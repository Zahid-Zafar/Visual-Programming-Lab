<Window x:Class="StudentProgressTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Student Progress Tracker" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Filter Section -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Filter by Grade:" VerticalAlignment="Center" />
            <ComboBox x:Name="GradeComboBox" Width="120" Margin="5,0" />
            <Label Content="Filter by Subject:" VerticalAlignment="Center" />
            <ComboBox x:Name="SubjectComboBox" Width="120" Margin="5,0" />
            <Button Content="Apply Filter" Width="100" Margin="5,0" Click="FilterButton_Click" />
        </StackPanel>

        <!-- DataGrid for Student Records -->
        <DataGrid x:Name="StudentDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Grade" Binding="{Binding Grade}" />
                <DataGridTextColumn Header="Subject" Binding="{Binding Subject}" />
                <DataGridTextColumn Header="Marks" Binding="{Binding Marks}" />
                <DataGridTextColumn Header="Attendance (%)" Binding="{Binding Attendance}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons for CRUD Operations -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Add Student" Width="100" Margin="5" Click="AddButton_Click" />
            <Button Content="Edit Student" Width="100" Margin="5" Click="EditButton_Click" />
            <Button Content="Delete Student" Width="100" Margin="5" Click="DeleteButton_Click" />
        </StackPanel>
    </Grid>
</Window>